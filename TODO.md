GIT GUI 기능
- git 명령어들을 UI에서 클릭으로 진행
- tui유지 + git gui툴 기능
- 구현된 Command들을 합쳐서 git gui 및 custom command 툴을 만드는것
- custom command로 기존 프로젝트(ChatService, AwsBillings 등등 에 있는 Makefile , run.ps1, make.ps1 등 명령어를 이툴로 대체)